# cryptography >= 48.0.0 is required: the vault uses ML-DSA-65 (FIPS 204),
# i.e. cryptography.hazmat.primitives.asymmetric.mldsa, which older releases
# (46.x and earlier) do not ship. Verified passing on 48.0.0.
cryptography>=48.0.0
argon2-cffi>=23.1.0
